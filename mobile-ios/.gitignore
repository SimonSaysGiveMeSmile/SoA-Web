# Dependencies (npm i) & Xcode build output — regenerated, never committed.
node_modules/
ios/App/build/
DerivedData/

# Bundled web client — generated by scripts/sync-web.js from ../web/public/m.
# Run `SOA_BACKEND=<url> node scripts/sync-web.js` to (re)populate.
www/

# Xcode user state / SPM pins (we repoint SPM to the local vendor; see README).
*.xcuserstate
**/xcuserdata/
**/Package.resolved
.DS_Store

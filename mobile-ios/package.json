{
  "name": "soa-mobile-ios",
  "version": "1.0.0",
  "private": true,
  "description": "Son of Anton — native iOS shell (Capacitor) bundling the /m/ mobile client",
  "scripts": {
    "sync-web": "node scripts/sync-web.js",
    "build": "npm run sync-web && npx cap sync ios",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1",
    "@capacitor/splash-screen": "^8.0.1",
    "@capacitor/status-bar": "^8.0.2"
  }
}
